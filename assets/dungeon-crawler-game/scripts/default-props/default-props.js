"use strict";

//local imports

const { events } = require("./events");
const { hoverInfo } = require("./hover-info");

//default props

const defaultProps = {
  events,
  hoverInfo
};

//exports

module.exports = { defaultProps };
