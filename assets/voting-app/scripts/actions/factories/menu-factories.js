"use strict";

//menu open form

const menuOpenForm = () => ({ type: "MENU_OPEN_FORM" });

//menu toggle confirm

const menuToggleConfirm = () => ({ type: "MENU_TOGGLE_CONFIRM" });

//exports

module.exports = {
  menuOpenForm,
  menuToggleConfirm
};
